from setuptools import find_packages, setup

setup(
    name="expense-tracker-demo",
    version="1.0.0",
    description="Small Python Expense Tracker for Jenkins and SonarQube practice",
    packages=find_packages(),
    python_requires=">=3.10",
)
